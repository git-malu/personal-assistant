# ── Agent Identity 配置 ──
# 生产环境通过 Agent Identity credential provider 获取模型 API key 和
# 用户授权的资源 token，Agent 代码与部署配置不直接保管密钥。
AGENTARTS_REGION=cn-southwest-2
# AgentArts Gateway 会通过 X-HW-AgentGateway-Workload-Access-Token 注入 workload token；
# SDK decorator 会从 AgentArtsRuntimeContext 中读取它。
# 本地调试 Agent Identity 时使用 agentarts-sdk 的本地配置（.agent_identity.json）。
# 私有 GitHub OAuth2 回调地址优先通过 AGENTARTS_GITHUB_OAUTH2_CALLBACK_URL 注入。
# AGENTARTS_GITHUB_OAUTH2_CALLBACK_URL=https://agent-identity.example.test/v1/oauth2/callback/redacted

# ── LLM Provider 配置 ──
# 通过 config.yaml 的 llm.default 选择默认 provider（maas 或 deepseek）。
# config.yaml 中的 api_key_provider 指向 Agent Identity API key provider：MAAS_API_KEY / DEEPSEEK_API_KEY。
# 本地 debug 可临时设置 MAAS_API_KEY 或 DEEPSEEK_API_KEY；生产环境不要在 .env 或部署配置中保存 LLM API key。
# 未设置本地 debug key 时，LLM API key 由 Agent Identity get_resource_api_key 按 provider 名称获取。
# MAAS_API_KEY=
# DEEPSEEK_API_KEY=
# 不要在 .env 中配置 M365 OAuth client secret 或华为云 AK/SK。
# M365 OAuth provider 尚未接入；接入时也应通过 Agent Identity OAuth provider 配置。
# 华为云 AK/SK 仅用于 GitHub Actions 部署侧鉴权，保存在 GitHub Secrets。

# ── Microsoft 365 OAuth2（Feature 10a 邮件工具）──
# 来源：Azure Portal → Microsoft Entra ID → 应用注册 → 证书和密码
# 需注册应用并授权 Microsoft Graph API 权限：Mail.Read, Mail.Send
# M365_CLIENT_ID=<azure-app-client-id>
# M365_CLIENT_SECRET=<azure-app-client-secret>
# M365_TENANT_ID=<azure-tenant-id>

# ── CORS 配置 ──
# 逗号分隔的允许 Origin 列表。未设置时默认仅允许 OBS 静态网站域名。
# 示例（单域名）：
#   CORS_ALLOWED_ORIGINS=https://personal-assistant-web-chat.obs-website.cn-southwest-2.myhuaweicloud.com
# 示例（多域名 — OBS + Netlify）：
#   CORS_ALLOWED_ORIGINS=https://obs-domain.example.com,https://<site-name>.netlify.app
# CORS_ALLOWED_ORIGINS=

# ── Checkpointer 后端选择 ──
# 设置 POSTGRES_DSN 优先使用 PostgresSaver（生产环境），
# 否则设置 SQLITE_DB_PATH 使用 AsyncSqliteSaver（本地持久化），
# 都不设置则默认使用 InMemorySaver（进程内存，重启丢失）。
# POSTGRES_DSN=postgresql://user:pass@host:5432/db
# SQLITE_DB_PATH=/path/to/checkpoint.db
