name = "agentarts-personal-assistant"
pages_build_output_dir = "./dist"
compatibility_date = "2026-06-18"
compatibility_flags = ["nodejs_compat"]

# Production bindings and environment variables are managed by OpenTofu in
# personal-assistant-infra/cloudflare.tf. Use .dev.vars for local Pages
# Functions configuration; do not add production credentials here.
